const { buildBlog } = require('./blog')
const { buildPodcast } = require('./podcast')
const { buildBoardOfDirectorsPage } = require('./board-of-directors')

module.exports = {
  buildBlog,
  buildPodcast,
  buildBoardOfDirectorsPage,
}
